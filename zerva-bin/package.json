{
  "name": "@zerva/bin",
  "version": "0.10.4",
  "description": "🌱 Zerva Command Line Tool",
  "keywords": [
    "server",
    "typescript",
    "event"
  ],
  "funding": {
    "type": "GitHub Sponsors ❤",
    "url": "https://github.com/sponsors/holtwick"
  },
  "license": "MIT",
  "author": {
    "name": "Dirk Holtwick",
    "email": "dirk.holtwick@gmail.com",
    "url": "https://holtwick.de"
  },
  "type": "module",
  "bin": {
    "zerva": "bin.cjs"
  },
  "scripts": {
    "build": "esbuild src/index.ts --platform=node --target=node10.4 --bundle --external:esbuild --external:node-notifier --minify --format=cjs --outfile=bin.cjs && chmod +x ./bin.cjs",
    "global": "pnpm -g ln",
    "prepublishOnly": "pnpm test && pnpm run build",
    "reset": "rm -rf node_modules pnpm-lock.yaml dist dist_www www",
    "start": "pnpm run watch",
    "test": "echo todo"
  },
  "dependencies": {
    "@types/node": "^18.0.6",
    "esbuild": "^0.14.49",
    "node-notifier": "^10.0.1"
  },
  "devDependencies": {
    "zeed": "^0.7.168"
  }
}
